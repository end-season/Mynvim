# Neovim Python 开发快捷键指南

## 通用快捷键

| 快捷键 | 功能 |
|--------|------|
| `<Space>` | Leader 键 |
| `jk` | 退出插入模式 |
| `<C-h>` | 切换到左侧窗口 |
| `<C-j>` | 切换到下方窗口 |
| `<C-k>` | 切换到上方窗口 |
| `<C-l>` | 切换到右侧窗口 |
| `<C-Up>` | 减小窗口高度 |
| `<C-Down>` | 增加窗口高度 |
| `<C-Left>` | 减小窗口宽度 |
| `<C-Right>` | 增加窗口宽度 |
| `<` | 保持选区向左缩进 |
| `>` | 保持选区向右缩进 |
| `<leader>e` | 打开/关闭文件浏览器 (Neotree) |
| `<c-\>` | 打开/关闭终端 |

---

## LSP 快捷键 (ty / pylsp)

| 快捷键 | 功能 |
|--------|------|
| `gd` | 跳转到定义 (Go to Definition) |
| `gr` | 查找引用 (Find References) |
| `K` | 显示悬浮提示 (Hover) |
| `<Space>rn` | 重命名符号 (Rename) |
| `<Space>f` | 格式化代码 |

---

## 补全快捷键 (Blink.cmp)

| 快捷键 | 功能 |
|--------|------|
| `<Tab>` | 选择下一个补全项 |
| `<S-Tab>` | 选择上一个补全项 |
| `<Up>` | 选择上一个补全项 |
| `<Down>` | 选择下一个补全项 |
| `<C-b>` | 向上翻看补全文档 |
| `<C-f>` | 向下翻看补全文档 |
| `<C-k>` | 显示/隐藏函数参数签名 |

---

## 诊断/报错快捷键

| 快捷键 | 功能 |
|--------|------|
| `gl` | 显示行内诊断信息 |
| `[d` | 跳转到上一个诊断 |
| `]d` | 跳转到下一个诊断 |
| `<Space>e` | 打开诊断列表 (Neovim 内置) |
| `<Space>q` | 跳转到上一个错误 |

---

## 注意事项

- Python 开发使用 `ty` (Rust-based LSP) 或 `pylsp`
- 项目根目录需要包含 `ty.toml`、`pyproject.toml` 或 `.git`
- 建议使用 `pyproject.toml` 定义项目结构
